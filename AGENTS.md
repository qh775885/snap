# 快门 (snap) · 项目协作与架构规则

本文件是「快门」项目的唯一 AI 协作与技术准则入口。任何后续协作均须严格遵守。

---

## 1. 软件定位与核心设计哲学
- **软件全称**：**快门** (英文/仓库: **`snap`**)
- **核心定位**：极简、高流畅度的本地视频取景与快门截图工具，专精**横屏截竖图**与**极速清点**。
- **坚决拒绝 AI**：绝不引入任何 AI 目标检测与自动跟踪代码。人眼构图与审美远胜死板算法，核心算力全部让渡给“机械式操控手感”。
- **三大核心交互生命线（必须死磕）**：
  1. **取景器（放映级丝滑步进）**：鼠标侧键/方向键快进快退必须连贯、均匀、丝滑，绝不跳帧或卡顿，确保肉眼看清每一帧微表情。采用“固定节拍 + seeked 门控”硬件级加速。
  2. **快门直裁（毫秒击发）**：支持全屏原图截取与预设构图直裁（9:16 / 3:4 / 1:1 / 4:5），鼠标右键或 `S` 键瞬间完成抓取+裁切+异步写盘，绝不打断预览。
  3. **靶场图库（极速清点）**：右侧双列网格完整原貌呈现；键盘流 `Del` / `Backspace` 瞬间物理删除废片并无缝切至下一张；支持双击进入大图看图器。

---

## 2. 技术栈与架构标准
- **包管理器**：强制统一使用 **`pnpm`**（严禁使用 npm 或 yarn，依赖由 `E:\.pnpm-store` 全局管理）。
- **前端核心**：**React 19 + Vite 7 + Tailwind CSS v4**。
  - 依赖解码库：`mpegts.js`（用于 MPEG-TS 流原生低延迟解封装播放）。
- **桌面宿主**：**Tauri 2.0 (Rust) + 系统自带 Windows WebView2**。
  - 彻底抛弃 Electron，整包免安装纯绿色版仅约 **22MB**，内存仅占用 ~25MB。
- **交付形态**：**仅交付免安装纯绿色便携版**（输出至 `release/快门-免安装绿色版/快门.exe`）。

---

## 3. 开发与构建工作流（铁律）
- **日常调细节 / 前端打磨**：
  - 用户启动命令：`pnpm dev`
  - 验证命令：`pnpm verify`（执行 `vite build` 静态类型和打包检查，耗时 3 秒以内）
  - **严禁**：在日常修改前端时频繁执行后台 Release 编译或强杀 WebView 进程！利用 Vite 的 ESM 毫秒级热更新，改完代码即时生效。
- **发布绿色版**：
  - 构建命令：`pnpm build`（脚本位于 `scripts/run-build.js`，编译 Rust 并将可执行文件及 `WebView2Loader.dll` 打包至 `release/快门-免安装绿色版`）。

---

## 4. 关键代码结构地图
```text
snap/
├── src/
│   ├── components/
│   │   ├── VideoStage.jsx   # 视频取景器主舞台、侧键长按平滑加速引擎、右键截图、时间轴进度条
│   │   └── Gallery.jsx      # 右侧靶场图库（双列网格、大图看图器、Del 秒删废片）
│   ├── App.jsx              # 顶栏操作区、构图比例切换、Tauri 原生拖拽事件监听
│   ├── bridge.js            # Tauri 2.0 原生 IPC 接口与 Web 降级封装
│   └── index.css            # Tailwind 样式注入
├── src-tauri/
│   ├── src/lib.rs           # Rust 原生命令（选择文件/目录、毫秒写盘、物理删图、媒体嗅探）
│   └── tauri.conf.json      # Tauri 窗口与安全策略配置（assetProtocol 全局直通）
├── scripts/
│   ├── run-dev.js           # 自动注入 w64devkit Rust 环境并启动 pnpm tauri dev
│   └── run-build.js         # 注入 PATH 并生成纯绿色免安装版
└── release/
    └── 快门-免安装绿色版/   # 最终独立免安装可执行程序（快门.exe + WebView2Loader.dll）
```

---

## 5. 操作映射标准
- **鼠标侧键（前 / Button 3）**：前进 1 帧（长按持续平滑加速推进）。
- **鼠标侧键（后 / Button 4）**：后退 1 帧（长按平滑倒退）。
- **鼠标右键**：毫秒快门截取（拦截系统右键菜单，截取并异步写盘）。
- **键盘 `Space`**：播放 / 暂停。
- **键盘 `←` / `→`**：单帧 / 步进微调。
- **键盘 `Del` / `Backspace`**：
  - 图库列表选中国中某张：瞬间物理删除该文件并自动切换选中下一张。
  - 大图看图器中：瞬间删除当前大图并无缝滑向下一张。
- **键盘 `Enter` / 双击卡片**：开启沉浸式高清全屏看图器（支持滚轮/方向键切图）。
