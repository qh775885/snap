# 项目协作规则 (快门 / snap)

本文件是项目唯一的 AI/协作主规则入口。后续任何协作 AI 必须先阅读并严格执行本规则。

## 1. 软件定位与设计哲学
- **软件名称**：**快门** (英文/仓库简写：**`snap`**)
- **核心定位**：极简、高流畅度的本地视频取景与快门截图工具，专精**横屏截竖图**与**极速清点**。
- **坚决拒绝 AI**：坚决不引入任何 AI 目标检测与自动跟踪代码。人眼构图与审美远胜死板算法，核心算力全部让渡给“极致顺手的机械式操控”。
- **三大核心交互生命线（必须死磕）**：
  1. **取景器（高刷平滑步进）**：按住方向键/侧键快进快退必须连贯、均匀、丝滑，绝不允许跳帧或卡顿，确保肉眼看清每一帧微表情与动作。必须遵守“固定节拍 + seeked 门控”机制。
  2. **快门直裁（毫秒击发）**：常驻横截竖构图框（9:16 / 3:4 / 1:1），按键（`S` 或鼠标快捷键）瞬间完成抓取+裁切+写盘，绝不打断预览。
  3. **靶场图库（极速清点）**：右侧列表实时排布截图；键盘流极速清理（`Del` 瞬间删除当前张、支持多选与批量清除废片）。

---

## 2. 技术架构与交付形态 (lite 分支)
- **交付形态**：**仅交付免安装纯绿色便携版**，不制作、不发布任何安装包。
- **宿主环境**：从 Electron 彻底迁移至 **Tauri 2.0 (Rust)**，前端借用系统 Windows WebView2，彻底移除 200MB 的 Chromium 负担。
- **解码引擎**：按需调用轻量裁剪版 FFmpeg 或直接利用系统硬解，体积严控在 25~30MB 左右。
- **前端代码**：保留现有的 React 19 + Tailwind CSS 高效渲染方案，仅做 IPC 桥接层适配与冗余代码收敛。

---

## 3. 明日动工指引（给下一次启动的 AI）
当前处于 **`lite`** 分支。按以下明确步骤推进：
1. **环境自检**：检查 Rust 工具链 (`rustc -V` / `cargo -V`)，若无则引导或使用 winget 安装。
2. **初始化 Tauri 2.0**：搭建 `src-tauri`，配置 `tauri.conf.json`。
3. **Rust Commands 实现**：替代原 `electron-main.js` 中的 5 个 IPC 接口（选择目录、系统打开路径、极简抽帧/写盘）。
4. **前端净化与重构**：
   - 抹平 `window.require('electron')` 为 Tauri invoke。
   - 彻底删除已过时的 AI 残留逻辑与无用旧配置。
   - 优化右侧图库：支持键盘 `Del` 单张直删、多选与批量删除。
5. **构建与自验**：构建纯绿色版产物，验证体积与操作手感。

---

## 4. 参考文档
- 核心技术规范：`docs/project-notes.md`
- 架构迁移方案：`docs/lite-plan.md`
- 进度追踪：`开发进度.md`
