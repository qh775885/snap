# 开发进度 (分支: lite)

> 当前版本定位：**快门 (snap)** 极简轻量化完全重构版

## 1. 核心定位与用户操作习惯
- **操作生命线**：
  - **鼠标侧键前/后**：丝滑连续步进快进/快退（单击单帧微调，长按平滑加速，绝不卡顿跳帧）。
  - **鼠标右键**：毫秒快门截取（自动拦截右键菜单，直裁写盘，零打断感）。
  - **靶场图库极速清点**：键盘 `Del` / `Backspace` 键单张秒删废片，支持多选与批量清除。
- **全格式与自由截图**：
  - 默认**全屏原比例截取**（任何横屏/竖屏视频，直接截取整张视频高清原画面）。
  - 自由构图裁切：一键切换 9:16（竖屏）、3:4（经典人像）、1:1（正方形）、4:5（社交贴），支持鼠标自由拖拽位置与滚轮缩放。
  - 分辨率预设：原画物理像素无损 / 1080P FHD / 720P HD。
- **交互与进度把控**：
  - 底部专业级可点击与拖动的交互式时间进度条，支持悬浮时间预览。
  - 播放/暂停控制、单帧微步进、当前时间/总时长毫秒级指示。
- **格式通吃与原生拖入**：
  - 集成 Tauri 2.0 原生窗口文件拖拽监听（100% 稳定响应外部文件拖入，彻底解决拖拽失效）。
  - 支持 MP4、MPEG-TS（集成 mpegts.js 内存零延迟解封装）、MKV、WebM 等。
- **极致轻巧**：**Tauri 2.0 (Rust) + 系统 WebView2**，免安装绿色单文件仅 **22MB**，内存占用仅 25MB！

---

## 2. 任务清单

### 准备阶段 (已完成)
- [x] 新建并切换至轻量化分支 `lite`
- [x] 彻底清理旧版 AI 跟踪、多余文档与旧规划
- [x] 确立新名称「快门 (snap)」并重构全局规范

### 重构落地与深度打磨阶段 (已全部完成)
- [x] **步骤 1**：环境检测并安装配置 Rust 编译工具链
- [x] **步骤 2**：搭建 Tauri 2.0 后端架构（`src-tauri`）
- [x] **步骤 3**：实现 Rust 轻量命令（选择文件夹/文件、系统打开路径、毫秒写盘、物理删除废片、媒体检测）
- [x] **步骤 4**：重构 `VideoStage.jsx`：鼠标侧键平滑提速步进引擎、鼠标右键快门直裁、交互式进度条与全屏原图/比例裁剪
- [x] **步骤 5**：重构 `Gallery.jsx`：大缩略图靶场、键盘 `Del` 单张秒删、批量多选清理与双击定位
- [x] **步骤 6**：重构 `App.jsx`：注册 Tauri 原生桌面文件拖入监听，默认全屏截取 + 多构图选项
- [x] **步骤 7**：彻底解决拖放与打包依赖，免安装绿色版产物验证（体积 22MB，秒开运行）
