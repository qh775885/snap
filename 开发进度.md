# 开发进度与里程碑 · 快门 (snap)

> 当前版本：**v2.2.0 (lite 分支)**  
> 运行平台：Windows 11 x64 (Tauri 2.0 Rust + 系统 WebView2)

---

## 🎯 核心架构与功能现状 (已全部落地)

### 1. 桌面宿主与底层交互
- [x] **Rust + WebView2 架构完全替换 Electron**：体积由 435MB 降至 **22MB**，冷启动 < 1s。
- [x] **Rust 原生 IPC 命令就绪**：选择文件/目录、文件毫秒写入、物理删除、系统外链打开。
- [x] **Tauri 2.0 窗口原生拖拽直通**：直接将外部 MP4/TS 视频文件拖入窗口即可毫秒级加载。
- [x] **MPEG-TS 硬解支持**：集成 `mpegts.js` 内存解封装，TS 文件流畅播放与步进。

### 2. 取景器与步进引擎 (`VideoStage.jsx`)
- [x] **鼠标侧键双向微调**：
  - 前侧键（Button 3）：单次前进 1 帧，长按固定节拍平滑加速。
  - 后侧键（Button 4）：单次倒退 1 帧，长按平滑倒退。
- [x] **鼠标右键瞬间击发快门**：拦截浏览器默认菜单，Canvas 内存直裁与异步落盘。
- [x] **全屏原图截取与多比例构图框**：默认 `full`（无黑边截取全屏高清画面），支持 `9:16`、`3:4`、`1:1`、`4:5` 构图框。
- [x] **专业时间轴交互**：支持精确点击寻道、拖拽洗带与毫秒时间指示。

### 3. 靶场图库与秒选清理 (`Gallery.jsx`)
- [x] **双列原比例完整网格**：自适应物理真实长宽比，拒绝强制方块拉伸或截断。
- [x] **新图置顶流**：最新抓拍置于首位，截图瞬间自动平滑置顶。
- [x] **秒删废片流水线**：单张选中按 `Del` / `Backspace` 瞬间从硬盘物理删除，焦点自动无缝滚入下一张。
- [x] **沉浸式大图看图器**：双击卡片即刻进入，支持方向键/滚轮翻页和无缝一键删除。

---

## 📌 后续体验精细打磨候选清单 (备忘)

- [ ] **快门声与视觉闪光微反馈**：可按需开启极微弱的“咔哒”快门机械音或 50ms 白色微闪。
- [ ] **自定义步进帧率**：允许根据视频帧率（24fps/30fps/60fps）精确设定单帧步进跨度。
- [ ] **快捷键自定义配置**：支持对习惯不同侧键顺序的用户提供侧键方向翻转选项。
