# 开发进度 (分支: lite)

> 当前版本定位：**快门 (snap)** 极简轻量化完全重构版

## 1. 核心定位与用户操作习惯
- **操作生命线**：
  - **鼠标侧键前/后**：丝滑连续步进快进/快退（单击单帧微调，长按平滑加速，绝不卡顿跳帧）。
  - **鼠标右键**：毫秒快门截取当前裁切框（自动拦截右键菜单，直裁写盘，零打断感）。
  - **靶场图库极速清点**：键盘 `Del` / `Backspace` 键单张秒删废片，支持多选与批量清除。
- **横屏截竖图**：常驻半透明构图框（9:16 / 3:4 / 1:1 / 4:5），支持原画无损物理像素与多种预设尺寸（1080P / 720P）。
- **格式通吃**：支持 MP4、MPEG-TS（集成 mpegts.js 内存零延迟解封装）、MKV、WebM 等。
- **极致轻巧**：彻底抛弃 400MB+ Electron，使用 **Tauri 2.0 (Rust) + 系统 WebView2**，免安装绿色单文件仅 **22MB**，内存占用仅 25MB！

---

## 2. 任务清单

### 准备阶段 (已完成)
- [x] 新建并切换至轻量化分支 `lite`
- [x] 彻底清理旧版 AI 跟踪、多余文档与旧规划
- [x] 确立新名称「快门 (snap)」并重构全局规范

### 重构落地阶段 (已全部完成)
- [x] **步骤 1**：环境检测并安装配置 Rust 编译工具链（支持 Windows MinGW GNU 编译目标）
- [x] **步骤 2**：搭建 Tauri 2.0 后端架构（`src-tauri`）并配置 `assetProtocol`
- [x] **步骤 3**：实现 Rust 轻量命令（选择文件夹/文件、系统打开路径、毫秒写盘、物理删除废片、媒体检测）
- [x] **步骤 4**：重构 `VideoStage.jsx`：鼠标侧键平滑提速步进引擎、鼠标右键快门直裁、常驻横截竖构图框
- [x] **步骤 5**：重构 `Gallery.jsx`：大缩略图靶场、键盘 `Del` 单张秒删、批量多选清理与双击定位
- [x] **步骤 6**：重构 `App.jsx`：极简清爽专业放映室风格、格式全兼容（集成 `mpegts.js`）
- [x] **步骤 7**：彻底剔除 Electron 依赖，完成纯绿色便携单文件编译构建（产物 `release/快门-免安装绿色版/快门.exe`，体积 22MB）
